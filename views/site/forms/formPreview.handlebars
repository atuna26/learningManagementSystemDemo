<div class="content-body" style="min-height: 1116px;">
    <div class="container-fluid">
        <!-- Add Project -->
        <div class="row page-titles mx-0">
            <div class="col-sm-6 p-md-0">
            </div>
            {{!-- <div class="col-sm-6 p-md-0 justify-content-sm-end mt-2 mt-sm-0 d-flex">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="javascript:void(0)">Home</a></li>
                    <li class="breadcrumb-item active"><a href="javascript:void(0)">Components</a></li>
                </ol>
            </div> --}}
        </div>
        <!-- row -->
        <div class="row d-flex justify-content-center">
            <div class="col-xl-6 col-xxl-6">
                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title">{{form.formName}}</h4>
                    </div>
                    <div class="card-body">
                        {{#each form.questAndAnswer}}
                        {{#if question}}
                        <div class="row d-flex">
                            <div class="form-group col-md-12">
                                <label class="mb-1"><strong>{{question}}</strong></label>
                                <input type="text" name="questAndAnswer.{{@index}}.answer" value="{{answer}}" readonly
                                    class="form-control">
                            </div>
                        </div>
                        <hr class="text-primary">
                        {{/if}}
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</div>
</div>